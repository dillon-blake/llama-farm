# `patches/`

Fork-local patch queue applied on top of the `vendor/llama.cpp` submodule. Owned by S0-02,
which defines the format and the `scripts/apply-patches.sh` driver.

Steady state, this queue is **empty**: real changes live as commits on the fork's
`learning-llamas-base` branch.
