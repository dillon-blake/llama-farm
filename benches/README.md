# `benches/`

Throughput and memory benchmarks. Filled by S1-32 (CPU throughput audit and the published
tok/s sizing table) and by each backend's milestone ticket (S2-10, S3-10, S4-09), which
publish a perf/memory snapshot alongside the convergence gate.
